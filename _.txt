データ駆動 + コンパイル時決定


scene追加は大変でもいい entity追加が大変なのは問題だ

bossはenemyの一種
stgはリンクする　<- 基盤が完成すれば

IRでrepeat,callは展開しない
spriteID = EntityType

call,None:	call,ret 	- callStack
repeat,end:	repeat,end 	- loopStack

stgのspawnのparamはeventTable.jsonを参照する

struct STGHeader {
    char magic[8];
    uint16_t version;
    uint32_t entry_pc;
    uint32_t code_size;
};

opcode = uint8_t
pc = uint32_t
vec2 = uint16_t
entityType = uint16_t

stg XMacro化成功
STGEntity::bezierEnemyはメモリレイアウトによって.stg.datを読むのに使う
cmd::bezierEnemyはintなどのengine内部での標準的な単位で表現される
この連携はVM::op_spawnで行われる
STGEntity:: はXMacroによって自動生成され、stgc/convert.pyも参照する
cmd:: は手動でstructを書き、engine内部に特化している
基本的にintはsignedであるべき


内部表現はvec2fで 描画もvec2f

entity update: true -> 生存, false -> 削除

enemy追加手順
	enemy/に.hでEntitytemplateを継承したクラスを作り、その.hをplayScene.cppに埋め込む
	shared/stgEntity.xmacにメモリ構成を定義
	shared/cmd.defにコマンドを記述

bullet追加手順
	bullet/に.hでEntitytemplateを継承したクラスを作り、その.hをplayScene.cppに埋め込む
	shared/cmd.defにコマンドを記述

描画システム内部の座標系は左上を原点に, 論理部分の座標系は中央を原点に

entity.def.jsonを廃止 entityTable.defに移行 imgはentity名と一致させる

角度管理型: 各Entityに MotionState structure
vec2f pos;
float angle;
float speed;

MotionPipeline mp;

angle = rad
degree = deg

GCMS = GameCommandMessageSystem

現在singlethreadなのでent=1500ぐらいまでなら耐えられる これ以上の弾幕にはmultithread化を考える必要あり?
	release buildの場合4000ぐらいまで50~60fpsで行けた 当分singlethreadで十分?
↓ release build
batch有効->
	4000 50~60
	10000 30?
batch無効->
	5000 50~60
	6000 30~40
ハイブリッド(採用)->
	5000~6000 60
	7000 30
当たり判定無効化->
	数万 60
当たり範囲処理が重すぎる

TextUI TUI用font描画システム コンストラクタで描画範囲をrectで指定する

同じSpriteのSpriteEntryは同じatlasに格納する

xmac def
----<名称>				<参照元>		<生成>
	sprite.def:			engine		自動(atlas.py)
	stgEntity.def		engine,stgc	手動
	sfx.def:			engine		手動
	cmd.def:			engine		手動
	
gcms
	cmd構造体 宣言 -> macro - gcms/gcms.h
			 定義 -> 各entityのcpp
	exe 宣言 -> macro - gcms/exec.h
		実装 -> 各entityのcpp
	variant -> macro - gcms/gcms.h

cmd::onHit: PlaySceneの担当では? gcmsを使用するのでcmdにgcmsの参照でも乗せるか?

EntityManagerのptr管理機能を分離して physWorldに移行

